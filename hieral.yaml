---
:backends:
  - yaml
:hierarchy:
  - "%{domain}
  - common

:yaml:
# datadir is empty here, so hiera uses its defaults:
# - /var/lib/hiera on *nix
# When specifying a datadir make sure it exists
  :datadir: /etc/puppetlabs/puppet/hieradata
